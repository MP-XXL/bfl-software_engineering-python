#Tuple: Ordered inmutable sequence of elements.
# Tuples are not mutable
# Tuples are declared using  parenthesis. ()
a = ("a",)
print(type(a))
#To have a single element become a tuple, use a trailing comma. Else printed; it will print type as list.
